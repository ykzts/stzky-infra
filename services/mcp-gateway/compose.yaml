services:
  gateway:
    container_name: mcp-gateway
    image: docker/mcp-gateway:v0.42.3@sha256:08a1798da195a3ed5770c1daa85b7cfbd1e2d852ff9da5c2f956422b08be8f21
    networks:
      - default
      - stzky-ingress
    command:
      - --transport=streaming
      - --servers=firecrawl,grafana
      - --port=8811
      - --static=true
    depends_on:
      - mcp-firecrawl
      - mcp-grafana
    restart: always

  mcp-firecrawl:
    container_name: mcp-firecrawl
    entrypoint: ['/docker-mcp/misc/docker-mcp-bridge', 'node', 'dist/index.js']
    image: mcp/firecrawl:latest@sha256:bd3d3a3a71a7803b61255447072db65835fe82558749ac2297a33806da3a327e
    environment:
      FIRECRAWL_API_URL: ${FIRECRAWL_API_URL:-http://firecrawl:3002}
      FIRECRAWL_API_KEY: ${FIRECRAWL_API_KEY:-}
    networks:
      - default
      - stzky-ingress
    cpus: 1
    init: true
    mem_limit: 2g
    security_opt:
      - no-new-privileges
    labels:
      - docker-mcp=true
      - docker-mcp-tool-type=mcp
      - docker-mcp-name=firecrawl
      - docker-mcp-transport=stdio
    volumes:
      - type: image
        source: docker/mcp-gateway
        target: /docker-mcp
    restart: always

  mcp-grafana:
    container_name: mcp-grafana
    entrypoint: ['/docker-mcp/misc/docker-mcp-bridge', '/app/mcp-grafana', '--transport', 'stdio']
    image: mcp/grafana:latest@sha256:3ad1ef6d41ea0f69ca38a50f89f64b2a3b804d910eb9e397e0735cc677a14cbe
    environment:
      GRAFANA_API_URL: ${GRAFANA_API_URL:-http://grafana:3000}
      GRAFANA_SERVICE_ACCOUNT_TOKEN: ${GRAFANA_SERVICE_ACCOUNT_TOKEN:-}
    networks:
      - default
      - stzky-ingress
    cpus: 1
    init: true
    mem_limit: 2g
    security_opt:
      - no-new-privileges
    labels:
      - docker-mcp=true
      - docker-mcp-tool-type=mcp
      - docker-mcp-name=grafana
      - docker-mcp-transport=stdio
    volumes:
      - type: image
        source: docker/mcp-gateway
        target: /docker-mcp
    restart: always

networks:
  stzky-ingress:
    external: true
