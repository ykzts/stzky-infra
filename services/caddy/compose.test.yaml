services:
  caddy:
    healthcheck:
      test: ['CMD', 'caddy', 'version']
      interval: 10s
      timeout: 5s
      retries: 5
    ports: []
    volumes:
      - ./config/caddy/Caddyfile.test:/etc/caddy/Caddyfile:ro
